<%= form_for([@pond, @frog]) do |f| %>
  <%= f.label :name, "Name " %>
  <%= f.text_field :name %>
  <%= f.label :color, "Color " %>
  <%= f.text_field :color %>
  <%= f.submit %>
<%end%>
<a href="/frogs">Cancel</a>
